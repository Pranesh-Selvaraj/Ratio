<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.NoActionBar">
        <item name="android:colorPrimary">@color/ratio</item>
        <item name="android:colorPrimaryDark">@color/ratio</item>
        <item name="android:colorAccent">@color/ratio</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:colorControlActivated">@color/ratio</item>
    </style>

</resources>
